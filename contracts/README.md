# Stable Contracts

- `ligand_graph.py`: active atom and bond dimensions.
- `biological_context.py`, `mechanism.py`: retained compatibility schemas.
- `source_affinity/`: governed source-affinity SQL/JSON contracts.

Changes here are schema changes, not ordinary model refactors. Update builders,
consumers, manifests, and contract tests together.
