# Raw Data

This directory stores immutable, source-format datasets only. `dta/` contains
the DAVIS and KIBA affinity benchmarks; other directories are grouped by their
upstream source. Derived artifacts belong in `dataset/processed/`.
