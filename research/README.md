# Research index

## Active frontier

Open-data source training is executable. The BindingDB Ki quotient corpus and
the first frozen 288D T-BASIS linear witness are complete. The witness returned
`CQ_TBASIS_LINEAR_AFFINITY_WITNESS_NOT_OBSERVED`.

The next stage is not yet preregistered. Its intended single axis is
target-coefficient heterogeneity with `d<=5`, trained from dense profiling
panels and evaluated as target-held-out `k=1/2/3/5` support episodes.

Current code:

- `crossed_interaction/bindingdb_cq_r0.py`: governed metadata projection;
- `crossed_interaction/bindingdb_cq_r1.py`: exact endpoint extraction/audit;
- `crossed_interaction/prepare_bindingdb_cq_corpus.py`: strict conflict closure;
- `crossed_interaction/quotient_operator.py`: complete-panel quotient;
- `crossed_interaction/generate_tbasis_features.py`: frozen CUDA feature path;
- `crossed_interaction/train_cq_linear.py`: panel-balanced linear witness.

Historical structural and failed routes remain in their original directories
for auditability. They are not active execution instructions. Nothing in
`archive/` is current evidence or authority.

Research code may enter `model/` or `scripts/` only after source-affinity,
partner-control, independent-confirmation and support-identifiability Gates.
