# Meta-few-shot evidence

This directory contains only:

- `stageR0` through `stageR14`: the governed current research cycle;
- `stageM0_msa_probe_20260816`: an independent, not-yet-run diagnostic proposal;
- `LEGACY_PRE_R0_SUMMARY.md`: consolidated verdicts for deleted earlier work.

The current Pareto set is A0/B3/C2; fixed Tanimoto is the k>=2 comparator.
No retained stage demonstrates an excellent or SOTA candidate. Leaf artifacts are
development evidence unless explicitly identified otherwise. Do not infer that a
directory containing `meta_test` belongs to the current sealed double-cold protocol;
the boundary report distinguishes older and quarantined populations.

New stages must follow the retention rule in `docs/PROJECT_FILE_ORGANIZATION.md`.
