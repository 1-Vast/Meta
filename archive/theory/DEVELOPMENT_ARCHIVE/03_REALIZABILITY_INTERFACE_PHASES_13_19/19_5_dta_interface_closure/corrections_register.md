# Corrections Register (Phase-19 Audit Absorption)

> **Status:** Phase-19.5, 2026-08-03. Before the DTA closure, the Phase-19 audit's theorem-level corrections are absorbed by retraction-and-restatement — none may survive silently into the handoff. Audit of record: `../19_final_interface_audit/FINAL_VERDICT.md` (`META_INTERFACE_INCOMPLETE`). New results **DT-0.x**, tagged **[retracted] / [restated] / [proved]**.

---

| # | Refuted statement | Correction |
|---|---|---|
| DT-0.1 | **MR-4 "no fixed $p$ across skeletons" — false as stated**: measurable (even continuous) maps from low-dimensional parameter spaces can surject onto higher-dimensional cubes (space-filling curves); parameter dimension alone yields no covering bound. **[retracted]** | **[restated]** The impossibility holds under an added regularity hypothesis: for families whose parameter-to-operator map has a uniform modulus of continuity (equivalently bounded description complexity at each scale) — the case for any family one would optimize over — metric entropy of the image is bounded per scale while the targets' entropy grows along refinements, and the argument stands. Without such regularity, no impossibility is claimed. Consequently MR-5's "weakest relaxation" is downgraded: **fixing the skeleton is a declared scope, provably necessary among uniformly-regular families, not proved necessary in general.** |
| DT-0.2 | **MI-6 minimality converse — false**: with a constant partition of unity and coinciding side channels, two distinct $z$ values receive identical outputs from every family member (audit's counterexample). **[retracted]** | **[restated]** Sufficiency of $r$ stands (definitional). Family-minimality of $r$ holds **iff** the declared map $z\mapsto(\varphi(z),\mathrm{confidence}(z),\mathrm{rung}(z))$ separates points — a checkable condition on the declared partition, *not* automatic, now listed as an optional deployment property (its failure wastes representation, never validity). |
| DT-0.3 | **MI-10 "minimizing set is a convex face" — false** ($\arg\min x^2$ on $[-1,1]$ is $\{0\}$, not a face); and convexity was asserted in the bilinear original coordinates. **[retracted]** | **[restated]** The optimization is formulated in the perspective variables $(\lambda,\tilde b_j)=(\lambda,\lambda b_j)$ on their compact convex feasible set, where the assembly is affine; the objective is then convex; the minimizer set is a nonempty **convex subset** (no face claim). Existence unchanged. |
| DT-0.4 | **Notation $r(S)$ imprecise** (index arguments are not support observations; permutation acts on $S$ only). | **[restated]** The representation is typed $r(S;Q,\gamma)$ with the permutation action on $S$ alone — adopting the audit's reading, which was already the proof's content. |
| DT-0.5 | **Grid-cell collision**: two distinct exact identified intervals inside one grid cell share finite bands, yet support restriction must differ — so the exact identification object cannot be left implicit in $r$. | **[restated, proved]** The deployed input carries the current identification object $I(S)$ (its interval/order data) as an **explicit typed component** alongside the band statistic (`operator_input_typing.md` DT-1); support restriction reads it directly. $r$'s stated dimension grows by the identified-interval coordinates — now typed, not smuggled. |
| DT-0.6 | **$b_{\mathrm{can}}(H)$ vs $b_{\mathrm{can}}(S_T)$ mismatch** (the audit's gap 1). | Resolved by the Part-1 typing decision (`operator_input_typing.md`): the two objects are *different* and both retained — the **population band** lives in the learned state $z_H$; the **identification band** $b_{\mathrm{id}}(S)$ is computed from the current support. Phase-18's conflation of the two under one symbol is retracted. |

These corrections change no validity result (the convex assembly, closed Route-B classes, ERM bound, and honesty firewalls stand as audited); they change *claims about* the construction — which is exactly what the handoff must state correctly.
