# Configuration

- `default.json`: local default configuration.
- `remote/`: machine-specific remote execution helpers and documentation.

Remote host files and private keys are local-only. Model experiment settings
must also be persisted with each result; this directory is not an experiment
ledger.
