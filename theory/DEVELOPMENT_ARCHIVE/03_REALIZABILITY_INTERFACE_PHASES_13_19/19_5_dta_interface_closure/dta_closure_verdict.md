# DTA Interface Closure — Verdict

> **Status:** Phase-19.5 terminal decision, 2026-08-03. Sources: the five closure files (DT-0–DT-9) and the audit `../19_final_interface_audit/FINAL_VERDICT.md` (`META_INTERFACE_INCOMPLETE`). No previous file modified; every audited correction absorbed by retraction-and-restatement (`corrections_register.md`); no arbitrary assumption added anywhere — each gap was closed either by a typing decision, a proof, or an explicit scope boundary with its impossibility witness.

---

## The decision

$$\boxed{\textbf{DTA\_META\_INTERFACE\_REQUIRES\_SCOPE\_LIMIT}}$$

The interface is **closed within a precisely drawn scope**, and the scope limits are not defects to be repaired later — two of them are theorems, one is a proved-irreducible obligation. Returning `CLOSED` without qualification would repeat the overclaim pattern that five consecutive audits corrected; the honest verdict names the boundary.

## Gap-by-gap resolution

| Audit gap | Resolution | Type |
|---|---|---|
| 1. $H$ vs $S$ mismatch | Input typed as **$A_\theta(z_H,S,Q,\gamma)$** (option B): history compressed into a frozen deployment state proved to be the sufficiency quotient; population band and identification band split into distinct typed objects; all four ambiguous readings decided (DT-1/2) | **closed fully** — typing decision + proof, no scope lost |
| 2. C3 undefined | Canonical $A^\star$ vs learnable $F_\omega$ separated claim-by-claim: validity/honesty/certificates unconditional by type; ERM conditional on tagged sampling; **(C3)** isolated as the single named implementation obligation with proved transfer constants and citable discharge for specified classical classes (DT-3/4) | **closed as an obligation boundary** — provably cannot be a theorem for unspecified classes |
| 3. Interval-censored calibration | Audit counterexample adopted as permanent witness; MI-12's censored-calibration claim retracted; **two-channel scope**: point-supervised tasks carry the (classically valid) elicitable interval score and all calibration claims; censored tasks feed only confidence-typed forced/compatible estimation; proved maximal without a censoring-mechanism axiom (DT-5/6) | **closed by scope limit** — larger scope requires a mechanism assumption the mandate forbids |
| 4. Continuous affinity ranking | **Impossibility proved** under current objects (marginal band classes cannot determine order laws — coupling witnesses); ranking typed as a **separate supervised objective** on the existing Route-A order object with its own identified supervision, vacuous-supervision degeneracy stated, cross-channel arrow made unrepresentable (DT-7/8) | **closed by scope limit** — a joint continuous-vector object would be new theory, declared out of scope |

Additionally, all six theorem-level corrections from the audit are registered and absorbed (space-filling-curve refutation of MR-4's generality; the constant-partition refutation of MI-6's minimality converse; the convex-face retraction and perspective-variable reformulation; the $r(S;Q,\gamma)$ notation; the grid-cell collision fix typing $I(S)$ explicitly; the $b_{\mathrm{can}}$ symbol split).

## The scope limits, stated exactly (the content of the verdict)

1. **Skeleton-relative deployment:** finite declared atlases/grid; refinement is re-deployment (necessity proved only among uniformly-regular families — DT-0.1's corrected status).
2. **Calibration only from point-identified supervision;** censored data contribute estimation, never calibration (DT-6: maximal honest scope).
3. **Ranking is separately supervised;** never derived from scalar affinity predictions (DT-7: impossible, not merely unimplemented).
4. **Two implementation obligations** — (C3) and optimization tolerance — are the entire unproved surface; everything else holds by type or by tagged theorem.

Within these four boundaries, `final_handoff_object.md` is a complete, directly implementable specification: every input, output, metric, loss, and assumption slot filled by a proved or declared-and-echoed object, with validity and honesty unconditional at every reachable implementation state.

**Verdict: `DTA_META_INTERFACE_REQUIRES_SCOPE_LIMIT`.**
