# External Review Helpers

This local-only directory contains K3 gateway helpers and checkpoint review
utilities. `llm.json` contains credentials and is explicitly ignored by Git.

External model output is advisory. It cannot modify the data protocol, replace
tests, or establish a performance claim without local governed experiments.
